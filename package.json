{
  "name": "app",
  "version": "1.0.0",
  "description": "A CLI simple app for student assessment reporting system",
  "scripts": {
    "test": "vitest --run --reporter verbose",
    "test:watch": "vitest",
    "app": "node app.js"
  },
  "keywords": [
    "CLI"
  ],
  "author": "Eddie Basharat",
  "license": "ISC",
  "devDependencies": {
    "vitest": "^0.34.6"
  },
  "dependencies": {
    "moment": "^2.29.4",
    "prompt-sync": "^4.2.0"
  }
}
